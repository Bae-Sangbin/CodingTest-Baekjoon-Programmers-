SELECT DISTINCT
    INF.REST_ID, 
    REST_NAME, 
    FOOD_TYPE, 
    FAVORITES, 
    ADDRESS, 
    ROUND(AVG(REVIEW_SCORE) OVER(PARTITION BY REST_ID),2) AS SCORE
FROM REST_INFO INF JOIN REST_REVIEW RV ON INF.REST_ID = RV.REST_ID
WHERE ADDRESS LIKE '서울%'
ORDER BY SCORE DESC, FAVORITES DESC;